@model SchoolEquipmentManagement.Web.ViewModels.Equipment.EquipmentChangeStatusViewModel

@{
    ViewData["Title"] = "Смена статуса";
}

<section class="hero-panel">
    <h1 class="display-title">Смена статуса</h1>
    <p class="page-intro">Выберите новый статус для оборудования и при необходимости оставьте комментарий.</p>
    <div class="hero-actions">
        <a asp-action="Details" asp-route-id="@Model.EquipmentId" class="btn btn-outline-secondary">К карточке</a>
    </div>
</section>

<section class="surface-card mb-3">
    <div class="inline-summary">
        <span>Инвентарный номер: <strong>@Model.InventoryNumber</strong></span>
        <span>Наименование: <strong>@Model.Name</strong></span>
        <span>Текущий статус: <strong>@Model.CurrentStatus</strong></span>
    </div>
</section>

<section class="surface-card">
    <form asp-action="ChangeStatus" method="post" novalidate>
        @Html.AntiForgeryToken()
        <input asp-for="EquipmentId" type="hidden" />
        <input asp-for="InventoryNumber" type="hidden" />
        <input asp-for="Name" type="hidden" />
        <input asp-for="CurrentStatusId" type="hidden" />
        <input asp-for="CurrentStatus" type="hidden" />

        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

        <div class="mb-3">
            <label asp-for="NewStatusId" class="form-label"></label>
            <select asp-for="NewStatusId" asp-items="Model.AvailableStatuses" class="form-select">
                <option value="">Выберите статус</option>
            </select>
            <span asp-validation-for="NewStatusId" class="text-danger"></span>
        </div>

        <div class="mb-4">
            <label asp-for="Comment" class="form-label"></label>
            <textarea asp-for="Comment" class="form-control" rows="4" placeholder="Например: передано в ремонт после диагностики"></textarea>
            <span asp-validation-for="Comment" class="text-danger"></span>
        </div>

        <div class="d-flex gap-2 flex-wrap">
            <button type="submit" class="btn btn-primary">Сохранить</button>
            <a asp-action="Details" asp-route-id="@Model.EquipmentId" class="btn btn-outline-secondary">Отмена</a>
        </div>
    </form>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
